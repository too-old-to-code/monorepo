import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { PrimaryButton, ButtonProps, SecondaryButton } from "./Button";

<Meta
  title="Atoms/Button"
  component={PrimaryButton}
  argTypes={{
    variant: {
      control: {
        type: "radio",
        options: ["primary", "secondary"],
      },
    },
    disabled: {
      control: "boolean",
    },
  }}
/>

# Button

With `MDX` we can define a story for `Button` right in the middle of our Markdown documentation.

<Canvas>
  <Story name="Primary" args={{ children: "hello" }}>
    {(args) => <PrimaryButton {...args} />}
  </Story>
  <Story
    name="Secondary"
    args={{ variant: "secondary", children: "secondary" }}
  >
    {(args) => <SecondaryButton {...args} />}
  </Story>
</Canvas>
